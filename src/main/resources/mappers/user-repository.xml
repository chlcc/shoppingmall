<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
    
<mapper namespace="org.sup2is.repository.UserRepository">
	
	<insert id="createUser" parameterType="org.sup2is.model.User">
		INSERT INTO user (userId, password, userName, phone, email)
		VALUSE (#{userId}, #{password} , #{userName} , #{phone} , #{email})
	</insert>

</mapper>